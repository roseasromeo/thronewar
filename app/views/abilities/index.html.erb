<p id="notice"><%= notice %></p>

<h1>Abilities</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Gift</th>
      <th>Level</th>
      <th>Automatic?</th>
      <th>Short text</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @abilities.each do |ability| %>
      <tr class='body'>
        <td><%= ability.name %></td>
        <td><%= ability.gift %></td>
        <td><%= ability.level %></td>
        <td><%= ability.automatic %></td>
        <td><%= ability.short_text %></td>
        <td><%= link_to 'Show', ability %></td>
        <td><%= link_to 'Edit', edit_ability_path(ability) %></td>
        <td><%= link_to 'Destroy', ability, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Ability', new_ability_path %>
