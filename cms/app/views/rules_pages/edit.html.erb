<h1> Editing... </h1>

<%= form_for :rules_page, url: rules_page_path(@rules_page), method: :patch do |f| %>

  <% if @rules_page.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@rules_page.errors.count, "error") %> prohibited this rules page from being saved:
      </h2>
      <ul>
        <% @rules_page.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>

  <p>
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>

<%= link_to 'Back', rules_pages_path %>
